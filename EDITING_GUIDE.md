# מדריך עריכה לאתר Be Meditation

## תוכן עניינים
1. [כללי](#כללי)
2. [שינוי תמונות](#שינוי-תמונות)
3. [עריכת טקסטים](#עריכת-טקסטים)
4. [עדכון פרטי יצירת קשר](#עדכון-פרטי-יצירת-קשר)
5. [שינוי צבעים](#שינוי-צבעים)
6. [עדכון גלריה](#עדכון-גלריה)
7. [הוספת תכונות](#הוספת-תכונות)

---

## כללי

האתר בנוי במערכת Next.js עם TypeScript. כל קובץ אחראי על חלק אחר:

### מבנה הקבצים העיקריים:
```
src/
├── components/sections/     # הסקשנים של האתר
│   ├── HeroSection.tsx     # הסקשן הראשי עם הכפתור
│   ├── AboutSection.tsx    # על הריטריט + "מה מחכה לנו"
│   ├── GallerySection.tsx  # גלריית התמונות
│   ├── ProgramSection.tsx  # לוח הזמנים
│   └── ContactSection.tsx  # צור קשר + פוטר
├── app/
│   ├── globals.css        # עיצוב כללי + צבעים
│   └── page.tsx          # סדר הסקשנים באתר
public/images/             # כל התמונות
```

---

## שינוי תמונות

### 1. תמונת הרקע הראשית (בהירו)
**קובץ:** `src/components/sections/HeroSection.tsx`
**שורה:** 29

```javascript
backgroundImage: 'url(/images/gallery/25.jpg)', // ← שנו את השם כאן
```

**איך להחליף:**
1. העלו תמונה חדשה לתיקייה `public/images/`
2. שנו את השם בקוד למעלה

### 2. תמונות בגלריה
**קובץ:** `src/components/sections/GallerySection.tsx`
**שורות:** 18-109

```javascript
const galleryImages = [
  {
    id: 1,
    src: '/images/gallery/1.jpg', // ← נתיב התמונה
    alt: 'נוף מדברי במצוקי דרגות', // ← תיאור לנגישות
    description: 'הנוף המרהיב ממקום הריטריט' // ← לא משמש כרגע
  },
  // הוסיפו תמונות נוספות כאן...
];
```

**איך להוסיף תמונה חדשה:**
1. העלו את התמונה לתיקייה `public/images/gallery/`
2. הוסיפו אלמנט חדש לרשימה:
```javascript
{
  id: 16, // המספר הבא ברצף
  src: '/images/gallery/התמונה_החדשה.jpg',
  alt: 'תיאור התמונה',
  description: 'תיאור מפורט' // לא נראה כרגע
},
```

### 3. תמונות רקע בסקשנים שונים
**קבצים שונים מכילים תמונות רקע:**

- **AboutSection.tsx, שורה 88:** תמונת רקע לציטוט
- **ProgramSection.tsx, שורה 144:** תמונת רקע לציטוט

---

## עריכת טקסטים

### 1. הכותרת הראשית
**קובץ:** `src/components/sections/HeroSection.tsx`
**שורות:** 50-55

```javascript
סופ״ש של נוכחות,
<br className="hidden sm:block" />
תנועה ושקט
<br className="hidden sm:block" />
במדבר
```

### 2. תת-כותרת עם תאריכים
**שורות:** 61-62

```javascript
ריטריט מדיטציה חווייתי ומעמיק | 26–27 בספטמבר | מצוקי דרגות
```

### 3. התיאור בהירו
**שורות:** 70-72

### 4. טקסט "על הריטריט"
**קובץ:** `src/components/sections/AboutSection.tsx`
**שורות:** 70-80

```javascript
מסע עומק וחופש לגוף ולנפש של סופ״ש במדבר יהודה הקסום והמרפא.
// המשך הטקסט...
```

### 5. פעילויות "מה מחכה לנו"
**שורות:** 17-54 באותו קובץ

```javascript
const experienceItems = [
  {
    id: 'meditation',
    icon: 'fas fa-om', // אייקון
    title: 'מדיטציות', // כותרת
    description: 'תגלו שיטות מדיטציה...' // תיאור
  },
  // פעילויות נוספות...
];
```

### 6. לוח הזמנים
**קובץ:** `src/components/sections/ProgramSection.tsx`
**שורות:** 16-96

```javascript
const fridaySchedule = [
  {
    time: '16:00', // שעה
    title: 'הגעה וקבלת פנים', // כותרת הפעילות
    description: 'הגעה למצוקי דרגות...' // תיאור
  },
  // פעילויות נוספות...
];
```

---

## עדכון פרטי יצירת קשר

**קובץ:** `src/components/sections/ContactSection.tsx`

### מספרי טלפון:
**חפשו בקובץ:** `054-7882715` ו-`+972547882715`

### קישורי WhatsApp:
**חפשו:** `https://wa.me/972547882715`

### קישור לרכישה:
**שורה 361:** `https://nataraj.co.il/mitzukei-retreat`

---

## שינוי צבעים

**קובץ:** `src/app/globals.css`

### הצבעים הראשיים:
**שורות:** 4-37

```css
:root {
  --cream: #f5eee6;        /* רקע ראשי */
  --sand: #E5D4B1;         /* חול */  
  --light-brown: #C7A882;  /* חום בהיר */
  --desert-blue: #7BA098;  /* תכלת מדברי */
  /* ... צבעים נוספים */
}
```

### צבע הכותרות (תכלת):
הצבע `#4A9EB3` מופיע במקומות רבים. חפשו אותו ושנו לפי הצורך.

### צבעי כפתורים:
רוב הכפתורים משתמשים ב-`#4A9EB3` ובhover: `#398EA0`

---

## עדכון גלריה

### הוספת תמונות:
1. העלו תמונות לתיקייה `public/images/gallery/`
2. ערכו את `GallerySection.tsx` (ראו למעלה)

### הסרת תמונות:
1. מחקו את האלמנט המתאים מהרשימה
2. תוכלו למחוק את קובץ התמונה (אופציונלי)

---

## הוספת תכונות

### הוספת רשימת תפוצה:

הקוד כבר מוכן בטופס. כרגע הוא שולח לדוגמה ב-`ContactSection.tsx`.

לחיבור אמיתי תצטרכו:
1. שירות כמו Make.com או Zapier
2. עדכון ה-URL בשורה 145:
```javascript
const response = await fetch('YOUR_WEBHOOK_URL', {
```

### שינוי סדר הסקשנים:
**קובץ:** `src/app/page.tsx`

פשוט שנו את הסדר של הקומפוננטים:
```javascript
<HeroSection />
<AboutSection />
<GallerySection />
// ... שנו את הסדר כרצונכם
```

---

## טיפים חשובים

### בדיקת שינויים:
1. שמרו את הקובץ
2. האתר יתעדכן אוטומטית (אם הוא רץ)
3. בדקו במובייל ובדסקטופ

### גיבוי:
לפני שינויים גדולים, גבו את הקבצים שאתם עורכים.

### נגישות:
תמיד עדכנו את שדה ה-`alt` בתמונות לתיאור מתאים.

### ביצועים:
תמונות כבדות (מעל 1MB) יכולות להאט את האתר. דחסו אותן לפני העלאה.

---

## צרו קשר לתמיכה

אם נתקלתם בבעיות או צריכים עזרה נוספת, התייעצו עם מפתח אתרים מקצועי.